[
  {
    "model_name": "granite",
    "deployable": true,
    "reason": "Deployable: Name suggests W4A16/W8A8-style quantization; treat as compatible pending runtime (verify AWQ/GPTQ paths). Minimum vLLM tensor-parallel-size \u2248 1 from ceil(4.59 GB / 40.0 GB per GPU) using model_size_gb (weights-only proxy when required_vram_gb missing; reserve KV cache separately); allocatable GPUs = 8. (Weights-only; tune max-model-len for KV cache.) Accelerator inferred from GPU Product as 'ampere' (not from VRAM size). Recommend --tensor-parallel-size=1 and gpu_count=1."
  }
]